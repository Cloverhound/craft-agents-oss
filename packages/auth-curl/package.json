{
  "name": "@craft-agent/auth-curl",
  "version": "0.3.4",
  "license": "Apache-2.0",
  "description": "Curl wrapper that auto-injects credentials from Craft Agent credential registry",
  "type": "module",
  "main": "src/cli.ts",
  "types": "src/cli.ts",
  "bin": {
    "auth-curl": "bin/auth-curl"
  },
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "@craft-agent/shared": "workspace:*"
  },
  "peerDependencies": {
    "zod": ">=3.0.0"
  }
}
