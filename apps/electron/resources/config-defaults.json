{
  "version": "1.0",
  "description": "Default configuration values for CloverhoundAI",
  "defaults": {
    "notificationsEnabled": true,
    "colorTheme": "default"
  },
  "workspaceDefaults": {
    "thinkingLevel": "think",
    "permissionMode": "safe",
    "cyclablePermissionModes": ["safe", "allow-all"],
    "localMcpServers": {
      "enabled": true
    }
  }
}
